server:
  port: 8081

app:
  jwt:
    secret: ${JWT_SECRET:studyplatform-local-dev-secret-must-be-changed-in-production}
    expiration: 1h
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  google:
    client-id: ${GOOGLE_CLIENT_ID:}
  mail:
    from: ${MAIL_FROM:no-reply@studyplatform.local}

spring:
  application:
    name: studyplatform-api

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: postgres.taulohcsqlvzdgougyky
    password: 32258953@@Paulo

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true

  flyway:
    enabled: true

  mail:
    host: ${SMTP_HOST:}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${SMTP_AUTH:true}
          starttls:
            enable: ${SMTP_STARTTLS:true}
