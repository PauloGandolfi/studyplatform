spring:
  datasource:
    url: jdbc:h2:mem:studyplatform_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none

app:
  jwt:
    secret: studyplatform-test-secret-with-enough-length
    expiration: 1h
  frontend-url: http://localhost:5173
  google:
    client-id:
  mail:
    from: no-reply@studyplatform.local
